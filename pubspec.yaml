name: phantom_fjord
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  flutter_bloc: 0.4.11
  get_it: ^1.0.2
  kiwi: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^0.10.1
  kiwi_generator: ^0.1.1

flutter:
  uses-material-design: true
